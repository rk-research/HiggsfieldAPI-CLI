# Repository instructions

For any request to create or manage visual assets with Higgsfield, use `.claude/skills/higgsfield-asset-generation/SKILL.md`. It covers the supported image/video workflows, presets, status, cancellation, estimates, downloads, and JSON result handling. Keep credentials in `.env` or the process environment, never in prompts or command arguments. Return the local downloaded file path from `outputs[].file`.
